Test .Net App with Redis and Docker.

I tried to cover most common Redis functions to illustrate how it works.

Also Docker file and docker-compose were created to launch redis and app instances in docker.
